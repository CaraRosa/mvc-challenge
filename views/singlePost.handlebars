<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{pageTitle}}</title>
  <link rel="stylesheet" href="/public/css/style.css">
</head>

<body>
  <header>
    <nav>
      <a href="/home">Home</a>
      <a href="/dashboard">Dashboard</a>
      {{#if logged_in}}
        <a href="/logout" id="logoutButton">Log Out</a>
      {{else}}
        <a href="/login">Login</a>
      {{/if}}
    </nav>
    <h1>{{pageTitle}}</h1>
  </header>

  <main class="container container-fluid mt-5">
    {{#each posts}}
    <article>
        <h2 class="post_title">{{title}}</h2>
        <p class="post_content">{{content}}</p>
        <p class="post_author">Author: {{author}}</p>
        <p class="post_date">Date: {{date}}</p>
        {{!-- Button for viewing a single post --}}
        <button class="viewFullPostButton" data-post-id="{{_id}}">View Full Post</button>
    </article>
    {{/each}}

  </main>

  <footer>

  </footer>
  <script src="/public/js/singlePost.js"></script>
</body>
</html>